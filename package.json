{
  "name": "con-management",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "npm start --prefix frontend",
    "api": "dotnet run --project backend",
    "frontend": "npm start --prefix frontend",
    "start:all": "concurrently -n api,web -c cyan,green \"npm run api\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
